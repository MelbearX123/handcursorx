# Environments
.venv/
__pycache__/
*.pyc

# Old YOLO work — keep code, ignore heavy data/artifacts
archive_yolo/Hand_Videos/
archive_yolo/dataset/
archive_yolo/runs/
archive_yolo/*.pt

# MediaPipe model bundle (downloaded, not committed — see README Setup)
models/*.task

# Future classifier pipeline (project #1)
data/*.csv
models/*.pkl
